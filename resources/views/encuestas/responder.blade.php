<!DOCTYPE html>
<html lang="es"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>{{ $encuesta->titulo }}</title><script src="https://cdn.tailwindcss.com"></script></head>
<body class="min-h-screen bg-slate-100 p-6"><main class="mx-auto max-w-3xl rounded-2xl bg-white p-8 shadow"><a class="text-sm text-sky-700" href="{{ route('eventos.index') }}">← Volver a eventos</a><h1 class="mt-4 text-2xl font-bold">{{ $encuesta->titulo }}</h1><p class="mt-2 text-slate-600">{{ $encuesta->instrucciones }}</p><form class="mt-8 space-y-6" method="POST" action="{{ route('encuestas.responder', $encuesta) }}">@csrf @foreach($encuesta->preguntas as $pregunta)<fieldset class="border-t pt-5"><legend class="font-medium">{{ $pregunta->enunciado }} @if($pregunta->es_obligatoria)<span class="text-red-600">*</span>@endif</legend>@if($pregunta->tipo_pregunta === 'ABIERTA')<textarea class="mt-3 w-full rounded border p-3" name="respuestas[{{ $pregunta->id }}]" @required($pregunta->es_obligatoria)>{{ old("respuestas.{$pregunta->id}") }}</textarea>@else<div class="mt-3 space-y-2">@foreach($pregunta->opciones as $opcion)<label class="flex gap-2"><input type="radio" name="respuestas[{{ $pregunta->id }}]" value="{{ $opcion->id }}" @checked(old("respuestas.{$pregunta->id}", $respuestas[$pregunta->id] ?? null) == $opcion->id) @required($pregunta->es_obligatoria)><span>{{ $opcion->texto_opcion }}</span></label>@endforeach</div>@endif</fieldset>@endforeach<button class="rounded-lg bg-sky-600 px-5 py-3 font-medium text-white">Enviar respuestas</button></form></main></body></html>
