@extends('layouts.app')
@section('title', 'Encuesta | Bienestar SENA')
@section('header-actions')<a href="{{ route('eventos.index') }}" class="rounded-lg bg-white/10 px-3 py-2 text-sm text-white">Volver a eventos</a>@endsection
@section('content')<div class="mx-auto max-w-3xl"><div class="mb-6"><span class="eyebrow text-xs font-bold uppercase text-[#648273]">Encuesta de satisfacción</span><h1 class="mt-2 text-3xl font-semibold">{{ $encuesta->titulo }}</h1><p class="mt-2 text-sm text-[#71847a]">{{ $encuesta->evento->nombre }}</p></div><form action="{{ route('encuestas.responder', $encuesta) }}" method="POST" class="space-y-5 rounded-2xl border border-[#d9e5dc] bg-white p-6 shadow-sm">@csrf @foreach($encuesta->preguntas as $index => $pregunta)<fieldset class="rounded-2xl border border-[#d9e5dc] bg-[#f8fbf8] p-5"><legend class="font-semibold">{{ $index + 1 }}. {{ $pregunta->enunciado }}</legend>@if($pregunta->tipo_pregunta === 'ESCALA_1_5')<div class="mt-4 grid grid-cols-5 gap-2">@for($i = 1; $i <= 5; $i++)<label class="rounded-xl border border-[#d9e5dc] bg-white p-3 text-center text-xs hover:border-[#9dbbaa]"><input type="radio" name="respuestas[{{ $pregunta->id }}]" value="{{ $i }}" required class="h-4 w-4 text-[#1d6b3d]"><span class="mt-1 block">{{ $i }}</span></label>@endfor</div>@elseif($pregunta->tipo_pregunta === 'ABIERTA')<textarea name="respuestas[{{ $pregunta->id }}]" rows="3" required class="mt-4 w-full rounded-xl border border-[#cddbd1] bg-white p-3 outline-none focus:border-[#23734d]" placeholder="Escribe tu opinión"></textarea>@endif</fieldset>@endforeach<div class="flex justify-end gap-3 border-t border-[#edf2ee] pt-5"><a href="{{ route('eventos.index') }}" class="rounded-xl bg-[#f0f5ef] px-5 py-3 text-sm font-semibold text-[#426b53]">Cancelar</a><button type="submit" class="rounded-xl bg-[#1d6b3d] px-5 py-3 text-sm font-semibold text-white hover:bg-[#155630]">Enviar encuesta</button></div></form></div>@endsection
